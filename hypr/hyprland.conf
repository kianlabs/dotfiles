

# This config is a STUB! This should never be generated.
# Use the default lua config from https://github.com/hyprwm/Hyprland/blob/main/example/hyprland.lua

autogenerated = 1

$mainMod = SUPER
$terminal = kitty
$fileManager = dolphin
$menu = hyprlauncher

bind = $mainMod, Q, exec, $terminal
bind = $mainMod, C, killactive,
bind = $mainMod, M, exec, command -v hyprshutdown >/dev/null 2>&1 && hyprshutdown || hyprctl dispatch exit
bind = $mainMod, E, exec, $fileManager
bind = $mainMod, V, togglefloating,
bind = $mainMod, R, exec, $menu

# >>> jcode launch hotkeys (managed) >>>
# jcode: home
bind = SUPER, semicolon, exec, '/home/k14n/.jcode/hotkey/launch_jcode_0_cmd_semicolon.sh'
# jcode: last project
bind = SUPER, apostrophe, exec, '/home/k14n/.jcode/hotkey/launch_jcode_1_cmd_quote.sh'
# jcode: self-dev
bind = SUPER SHIFT, apostrophe, exec, '/home/k14n/.jcode/hotkey/launch_jcode_2_cmd_shift_quote.sh'
# <<< jcode launch hotkeys (managed) <<<
